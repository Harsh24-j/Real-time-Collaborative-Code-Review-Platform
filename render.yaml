services:
  # ── Backend Spring Boot Service ────────────────────────────────────────
  - type: web
    name: codereview-backend
    env: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    region: oregon
    plan: free
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: prod
      - key: DATABASE_URL
        fromDatabase:
          name: codereview-db
          property: jdbcUrl
      - key: JDBC_DATABASE_URL
        fromDatabase:
          name: codereview-db
          property: jdbcConnectionString
      - key: DATABASE_USERNAME
        fromDatabase:
          name: codereview-db
          property: user
      - key: DATABASE_PASSWORD
        fromDatabase:
          name: codereview-db
          property: password
      - key: JWT_SECRET
        generateValue: true
      - key: OPENAI_API_KEY
        sync: false
      - key: PORT
        value: 8080
      - key: ALLOWED_ORIGINS
        value: https://codereview-frontend-ttjg.onrender.com

  # ── Frontend React Static Site ─────────────────────────────────────────
  - type: web
    name: codereview-frontend
    env: static
    buildCommand: npm ci && npm run build
    staticPublishPath: dist
    rootDir: frontend
    pullRequestPreviewsEnabled: true
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        value: https://codereview-backend-i12l.onrender.com
      - key: VITE_WS_URL
        value: wss://codereview-backend-i12l.onrender.com/ws-review

databases:
  # ── PostgreSQL Database ────────────────────────────────────────────────
  - name: codereview-db
    region: oregon
    plan: free
    databaseName: codereview
    user: codereview_user
